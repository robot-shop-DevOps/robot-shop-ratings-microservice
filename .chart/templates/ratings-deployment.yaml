apiVersion: apps/v1

kind: Deployment

metadata:
  name: {{ .Values.ratings.microservice }}-deployment
  labels:
    app.kubernetes.io/name: {{ .Chart.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/Version: {{ .Chart.Version }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/component: {{ .Values.ratings.microservice }}
    {{- range $key,$value := .Values.ratings.labels }}
    {{ $key }}: {{ $value }}
    {{- end }}

spec:
  replicas: {{ .Values.ratings.replicas }}
  strategy: 
{{ .Values.ratings.strategy | toYaml | indent 4 }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Chart.Name }}
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/Version: {{ .Chart.Version }}
      app.kubernetes.io/managed-by: {{ .Release.Service }}
      app.kubernetes.io/component: {{ .Values.ratings.microservice }}
      {{- range $key,$value := .Values.ratings.labels }}
      {{ $key }}: {{ $value }}
      {{- end }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ .Chart.Name }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/Version: {{ .Chart.Version }}
        app.kubernetes.io/managed-by: {{ .Release.Service }}
        app.kubernetes.io/component: {{ .Values.ratings.microservice }}
        {{- range $key,$value := .Values.ratings.labels }}
        {{ $key }}: {{ $value }}
        {{- end }}
    spec:
      containers:
      - name: {{ .Values.ratings.microservice }}
        image: "{{ .Values.ratings.container.image.repo }}:{{ .Values.ratings.container.image.version }}"
        imagePullPolicy: {{ .Values.ratings.container.image.imagePullPolicy }}
        ports: 
        - containerPort: {{ .Values.ratings.container.port }}
        readinessProbe:
{{ .Values.ratings.container.readinessProbe | toYaml | indent 10}}